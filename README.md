# C-toolkit-Project
Built a C toolkit that applies weighted-average scoring across three real-world scenarios — employee performance reviews, weather severity ranking, and scholarship eligibility — all powered by one shared, reusable scoring engine instead of duplicated logic per use case.
